/*import {
    Column,
    Entity,
    PrimaryGeneratedColumn,
} from 'typeorm';

@Entity({ name: 'org_section'})
export class OrgSection {
    @PrimaryGeneratedColumn()
    id: number;

    @Column("integer", {nullable: false})
    org_id: number;

    @Column("integer", {nullable: false})
    section_id: number;
}

export class OrgSectionTmp {
    org_id: number;
    section_id: number;
}*/